server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/admin_db?createDatabaseIfNotExist=true
    username: root
    password: your_password_here
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
